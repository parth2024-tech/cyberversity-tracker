# Fly.io configuration for AetherGuard AI Security Monitor
# Zero-cost persistent 24/7 deployment

app = "aether-guard-monitor"
primary_region = "iad"

[build]
  dockerfile = "Dockerfile"

[env]
  PYTHONUNBUFFERED = "1"
  API_HOST = "0.0.0.0"
  API_PORT = "8080"
  SCHEDULER_ENABLED = "true"
  DATA_DIR = "/app/data"
  DB_URL = "sqlite+aiosqlite:////app/data/monitor.db"
  DB_RETENTION_DAYS = "7"

[mounts]
  source = "aether_data"
  destination = "/app/data"
  initial_size = "1gb"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1

[[http_service.checks]]
  grace_period = "15s"
  interval = "30s"
  method = "GET"
  timeout = "5s"
  path = "/api/health"

# Pin to free shared-cpu-1x (256MB RAM) — keeps machine alive 24/7 on free tier
[[vm]]
  size = "shared-cpu-1x"
